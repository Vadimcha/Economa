import {Layout} from "../../components/Layout";
import {Title} from "@mantine/core";

export const AboutUsPage = () => {
    return (
        <Layout>
            <Title order={3}>О нас</Title>
        </Layout>
    );
};